@file:Suppress("ObjectPropertyName")

// @generated by Style Dictionary from newui/tokens/*.tokens.json — 禁止手改
package com.mini.me_core.newui.designsystem.token.generated

import androidx.compose.animation.core.CubicBezierEasing
import androidx.compose.animation.core.Spring
import androidx.compose.animation.core.SpringSpec
import androidx.compose.animation.core.spring
import androidx.compose.animation.core.tween
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

object AppColor {
    val BrandPrimary = Color(0xFF0A84FF) // 强调色 · iOS 系统蓝（主色调定板）
    val BrandSurface = Color(0xFFF2F2F7) // 系统分组底（浅）iOS systemGroupedBackground
    val BrandSurfaceDim = Color(0xFFE9E9EE) // 次要面/分隔（浅）
    val BrandCard = Color(0xFFFFFFFF) // 分组卡片/成员表面（浅）
    val BrandInk = Color(0xFF111114) // 一级文字 label.primary（浅）
    val BrandAccent = Color(0xFF5AC8FA) // 点缀蓝（iOS 浅蓝 accent）
    val LabelSecondary = Color(0xFF8E8E93) // 二级文字 label.secondary
    val LabelTertiary = Color(0xFFC7C7CC) // 三级文字 label.tertiary
    val StatusSuccess = Color(0xFF34C759) // iOS 系统绿
    val StatusWarning = Color(0xFFFF9F0A) // iOS 系统橙
    val StatusDanger = Color(0xFFFF3B30) // iOS 系统红
    val StatusInfo = Color(0xFF0A84FF) // iOS 系统蓝
    val SeparatorOnLight = Color(0xFFDCDCE0) // 分割线（浅）
    val SeparatorOnDark = Color(0xFF38383A) // 分割线（深）
    val OnDarkPrimary = Color(0xFF3D9BFF) // 暗色强调蓝（提亮一档）
    val OnDarkSurface = Color(0xFF000000) // 系统分组底（深）iOS systemBackground
    val OnDarkSurfaceDim = Color(0xFF2C2C2E) // 三级层次面（深）iOS tertiarySystemBackground
    val OnDarkSurfaceRaised = Color(0xFF1C1C1E) // 分组卡片（深）iOS secondarySystemGroupedBackground
    val OnDarkInk = Color(0xFFFFFFFF) // 一级文字（深）
    val OnDarkSecondaryLabel = Color(0xFF98989D) // 二级文字（深）
    val OnPrimary = Color(0xFFFFFFFF) // primary 上的内容色（浅=白）
    val OnPrimaryDark = Color(0xFF000000) // primary 上的内容色（深=黑）
    val PrimaryOverlay12 = Color(0x1F0A84FF) // primary 12% 覆盖层（浅）
    val PrimaryOverlay14 = Color(0x240A84FF) // primary 14% 覆盖层（浅）
    val PrimaryOverlay12Dark = Color(0x1F3D9BFF) // primary 12% 覆盖层（深）
    val PrimaryOverlay14Dark = Color(0x243D9BFF) // primary 14% 覆盖层（深）
    // 能力标签组
    val CapVision = Color(0xFF0A84FF) // 能力标签·识图（iOS 系统蓝）
    val CapTools = Color(0xFF34C759) // 能力标签·工具调用（iOS 系统绿）
    val CapReasoning = Color(0xFFBF5AF2) // 能力标签·推理（iOS 系统紫）
    val CapWarm = Color(0xFFFF9F0A) // 能力标签·通用暖金（badge 标签）
    // 对话模式组
    val ModeBuild = Color(0xFFB8860B) // 对话模式·构建（金棕）
    val ModePlan = Color(0xFF0A84FF) // 对话模式·计划（iOS 系统蓝）
    val ModeAuto = Color(0xFFFF3B30) // 对话模式·自动（iOS 系统红）
    // 数学/公式
    val MathInline = Color(0xFFBF5AF2) // 行内数学公式 $...$（紫）
    // 暗色点缀
    val OnDarkAccent = Color(0xFF64D2FF) // 暗色点缀蓝（DarkPalette accent 收编）
}

object AppSpacing {
    val Tiny = 2.dp
    val Xs = 4.dp
    val Sm = 8.dp
    val Md = 12.dp
    val Lg = 16.dp
    val Section = 20.dp // 卡片/列表区间距（介于 Lg 与 Xl 之间）
    val Xl = 24.dp
    val Xxl = 32.dp
}

object AppRadius {
    val None = 0.dp
    val Sm = 8.dp
    val Md = 12.dp
    val Lg = 16.dp
    val Pill = 999.dp
}

/** 边框/stroke 描边宽度专用。 */
object AppStroke {
    val Thin = 1.dp
}

object AppElevation {
    val Z0 = 0.dp // 静止（无阴影，靠内容色分层）
    val Z1 = 1.dp
    val Z2 = 3.dp // 滚动抬升
    val Z3 = 8.dp
    val Z4 = 12.dp
}

object AppSizing {
    val TouchTarget = 44.dp
    val IconButton = 40.dp
    val IconXs = 16.dp
    val IconS = 18.dp
    val IconM = 20.dp
    val IconL = 24.dp
    val IconXl = 28.dp // 卡片操作图标钮 / 中号头像
    val IconBlock = 40.dp // 图标块规格
    val Fab = 56.dp // 中央主操作圆钮直径（与扇形项圆形图标块同规格）
}

object AppLayout {
    val PageHorizontal = 16.dp
    val BlockGap = 16.dp
    val RowGap = 8.dp
    val ContentMaxWidth = 720.dp
    val DividerThickness = 1.dp // 分割线统一厚度
    val BottomBarHeight = 56.dp // 门户底栏总高（不含系统导航/手势 inset）
    val ChatUserBubbleMaxWidth = 260.dp // 用户气泡最大宽度（右对齐紧凑小气泡）
    val ChatAssistantBubbleMaxWidth = 300.dp // AI 气泡最大宽度（左对齐，头像右侧）
}

/** 字号令牌（sp）：组件统一用 [MaterialTheme.typography.*].copy(fontSize = AppType.*) 引用。 */
object AppType {
    val Caption = 11.sp // 极小号标签（底栏 tab / 扇形项文字）
}

object AppMotion {
    val Fast = 150L // 150ms
    val Med = 250L // 250ms
    val Slow = 400L // 400ms

    /** M3 Standard easing：进场/退场通用曲线。 */
    val EasingStandard = CubicBezierEasing(0.2f, 0.0f, 0f, 1.0f)
    /** M3 Emphasized easing：强调性过渡曲线。 */
    val EasingEmphasized = CubicBezierEasing(0.2f, 0.0f, 0f, 1.0f)
    /** 减速曲线：元素进场（先快后慢）。 */
    val EasingDecelerate = CubicBezierEasing(0.0f, 0.0f, 0.2f, 1.0f)

    /** 标准 tween（Fast 时长 + Standard easing）。 */
    fun <T> standardTween() = tween<T>(durationMillis = Fast.toInt(), easing = EasingStandard)
    /** 强调 tween（Med 时长 + Emphasized easing）。 */
    fun <T> emphasizedTween() = tween<T>(durationMillis = Med.toInt(), easing = EasingEmphasized)

    /** 标准弹簧（MediumBouncy 阻尼 + Medium 刚度），用于常规交互反馈。 */
    fun <T> standardSpring(): SpringSpec<T> = spring(
        dampingRatio = Spring.DampingRatioMediumBouncy,
        stiffness = Spring.StiffnessMedium,
    )
    /** 强调弹簧（MediumBouncy 阻尼 + MediumLow 刚度），用于更明显的弹性动画。 */
    fun <T> emphasizedSpring(): SpringSpec<T> = spring(
        dampingRatio = Spring.DampingRatioMediumBouncy,
        stiffness = Spring.StiffnessMediumLow,
    )
    /** 无回弹弹簧（NoBouncy 阻尼 + MediumLow 刚度），用于开关/滑块等精确控制。 */
    fun <T> noBounceSpring(): SpringSpec<T> = spring(
        dampingRatio = Spring.DampingRatioNoBouncy,
        stiffness = Spring.StiffnessMediumLow,
    )
}
