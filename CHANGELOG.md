# Changelog

本文件记录 MiniMe-core 的用户与开发者可见变更（**第 2 层 · 开发者视角**），采用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范；版本号遵循四段式语义（见 [AGENTS.md](./AGENTS.md#版本号规范)）。三层日志的分工与口径见 **AGENTS.md「版本日志」**。

- 最新版本置顶，倒序排列。
- 日期用 ISO 8601（`YYYY-MM-DD`）。
- 条目按「效果」而非「实现」撰写；内部噪音（纯格式、纯测试、非行为 refactor）不收录。
- **Breaking Change 必须用 ⚠️ 显著标注并附迁移说明。**

## [Unreleased]

## [logviewer-0.0.6] - 2026-09-24

> MiniMe Logs 附属应用全面体验升级版本：长按菜单、设置页、崩溃页三大核心页面深度重构，功能从"可用"升级为"专业"。

### Added

- `[logs]` 长按菜单全面重构为底部 BottomSheet：图标+分组标题+状态感知，新增只显示/隐藏此Tag、只显示此等级、用消息搜索、分享此行/上下文、跳转时间等操作
- `[logs]` 长按菜单支持双击行快速复制消息内容，长按触发震动反馈
- `[logs]` 设置页新增字体大小（小/中/大/特大）、字体切换（系统默认/Monospace）、显示毫秒、自动尾随、默认文件模式（今天/记住上次/每次询问）、主题切换（跟随系统/浅色/深色/纯黑）全部真正生效
- `[logs]` 崩溃页新增搜索（按异常类型/消息/Tag/堆栈，关键词高亮）、ANR 识别、Native Crash 识别、忽略功能、24h 趋势徽标（恶化/好转/持平）、排序选项（次数/最近时间/首次时间）
- `[logs]` 崩溃详情页新增堆栈语法高亮（异常类型红/类名蓝/方法名粗/Caused by 红）、堆栈行可点击复制、长堆栈横向滚动、发生记录可点击、SAF 导出 .txt 文件、长按多选批量操作、24h 发生时间分布柱状图、相似崩溃提示
- `[logs]` 崩溃已修复/忽略状态持久化，重启不丢失
- `[logs]` 全局数据同步：切换日志目录后所有页面（日志/统计/崩溃）自动刷新，带 shimmer 骨架屏过渡

### Changed

- `[logs]` 等级筛选栏从字母缩写+分离计数改为完整名称（Error/Warn/Info/Debug/Verbose）+计数合并进同一 chip
- `[logs]` 崩溃卡片状态指示从 emoji 改为 Material 图标+左侧色条，详情按钮升级为 Filled 主操作
- `[logs]` 崩溃筛选从下拉菜单改为 4-Tab 横向切换（全部/未修复/已修复/已忽略），带实时计数
- `[logs]` 崩溃聚合 key 从"异常类型+堆栈首行（含行号）"改为"异常类型+消息前50字符"，同异常不同行号正确合并

### Fixed

- `[logs]` 尾随模式计数不准：异步加载导致 after-before 永远为 0，改为同步加载
- `[logs]` 日志列表排序：最新日志从顶部往下排列
- `[logs]` 长日志内容超出屏幕被截断：消息行支持横向滑动
- `[logs]` 三按钮弹窗中间按钮白底白字看不清：改为 Tonal 按钮
- `[logs]` 软件内版本号写死 1.0.0：改为读取 BuildConfig.VERSION_NAME
- `[logs]` 设置页 9 项摆设功能：移除 12小时制和清理7天前日志，导出项降级为"即将上线"明示，其余 6 项真正实现
- `[logs]` 智能识别主应用日志目录：SAF 选择器预定位到 Documents/MiniMe-core/，错误目录拒绝并引导

## [0.0.0.12] - 2026-09-23

> 日志系统全面升级版本：运行日志页面重构为专业日志查看器，支持等级筛选、文件选择、实时尾随、搜索高亮、横竖屏自适应；同时日志底层深度优化，写入性能大幅提升，新增 FATAL 等级、线程信息、格式版本头、敏感信息脱敏、实时统计等能力，为后续独立日志附属应用奠定基础。

### Added

- `[log]` 运行日志页面重构为专业日志查看器：结构化日志行（时间戳+等级徽章+Tag+消息+堆栈折叠），长按复制全文/消息/时间戳/Tag，关键词搜索实时高亮并支持上一处/下一处跳转
- `[log]` 等级筛选栏常驻显示各等级数量徽章：单击筛选该等级及以上，长按进入多选模式，再次点击折叠该等级所有行
- `[log]` 文件选择支持快捷范围（今天/近3天/近7天/全部）与自定义日期范围，文件列表显示大小并可多选合并查看
- `[log]` 实时尾随模式：新日志自动滚动到底部并绿色脉冲高亮，上滑自动暂停并显示「有 N 条新日志」浮动按钮，点击恢复尾随
- `[log]` 横屏自适应：≥600dp 自动切换左右分栏（左侧列表+右侧详情），竖屏保持堆叠布局
- `[log]` 日志等级新增 FATAL（致命错误），位于 ERROR 之上，用于标记不可恢复的严重故障
- `[log]` 日志行新增线程信息 `[thread:name]`，便于排查多线程并发问题
- `[log]` 日志文件新增格式版本头（`# MiniMe Log Format v1`），记录应用版本与进程 ID，为日志格式演进与兼容性检测提供依据
- `[log]` 新增实时统计能力：各等级计数、各 Tag 计数，运行时可查询
- `[log]` 新增耗时记录便捷方法 `timing(tag, label, durationMs)`

### Changed

- `[log]` 日志写入性能优化：从每条日志单独 open/write/close 改为内存队列缓冲 + 常驻 BufferedWriter，按 64KB 或 500ms 批量 flush，大幅降低 IO 开销
- `[log]` 文件滚动策略从「超限截断删除全部历史」改为非破坏性滚动（`log-date.txt → .1 → .2`），历史日志完整保留
- `[log]` 日志导出从全量 readText 入内存改为 8KB 流式复制，避免大文件导出时 OOM
- `[log]` 日志配置集中管理：日志级别、缓冲大小、文件上限、清理天数等统一由 LogConfig 管理，支持运行时覆盖
- `[log]` 日志层接口抽象：新增 Logger 接口与 TestLogger 实现，便于单元测试与未来扩展输出目标（网络/数据库）

### Fixed

- `[log]` 修复日志文件超过 5MB 时全部历史被截断删除的问题，改为滚动保留
- `[log]` 修复多线程并发写入日志时的锁竞争问题，改为单线程写入器串行化处理
- `[log]` 修复日志导出大文件时可能 OOM 的问题，改为流式复制

### Security

- `[log]` 敏感信息统一脱敏：日志写入前自动检测并脱敏 API Key、Access Token、密码、Authorization 头、长 base64 媒体数据等，避免敏感信息泄露到日志文件
- `[log]` 格式版本头为后续日志格式不兼容变更提供检测机制，附属应用可根据版本号判断是否需要升级

## [0.0.0.11] - 2026-09-23

> 工作台与对话绑定修复 + AI 回复消息持久化加固版本：修复删除工作台不级联删除对话、备份恢复丢失绑定关系等严重 Bug；同时为 AI 回复增加三层持久化防护，彻底解决应用被杀导致回复消息丢失的问题。

### Fixed

- `[workspace]` 删除工作台时级联删除绑定的对话及全部消息：删除确认框显示将同时删除的对话数量，提示"不可恢复"，避免产生数据库中存在但任何工作台都不可见的幽灵会话
- `[backup]` 备份恢复时保留会话原始工作台绑定关系：仅当原始路径为空时才兜底到当前工作台，不再强制全部覆盖
- `[workspace]` 工作台详情页"绑定对话"列表正确显示：回调已接线，可查看每个工作台绑定的对话
- `[workspace]` 切换工作台时自动选择该工作台下的最近会话，增加竞态保护：快速切换时旧查询结果不会覆盖新会话 ID
- `[agent]` AI 回复流式期间定期持久化：每累积 2000 字符或间隔 3 秒写入一次数据库，应用被杀时最多丢失最后几秒内容而非整条回复
- `[agent]` 消息持久化写入使用不可取消上下文：协程取消时已启动的数据库写入不会中断，所有持久化调用点自动受益
- `[agent]` 流式异常中断时兜底保存已收到的内容：处理流式刚开始就异常终止的极端情况，正常完成时不重复落库

### Security

- `[agent]` 三层持久化防护相互搭配：定期落库（主力）+ 不可取消写入（保障）+ 异常兜底（最后防线），彻底解决 AI 回复因应用被杀或协程取消而丢失的问题

## [0.0.0.10] - 2026-09-23

> 对话流与设置体验深度打磨版本：基于真机验收逐项修复 23 项问题。新增会话列表左滑快捷操作、长文本半屏↔全屏展开编辑面板、模型输出弹性跟随滚动与回到底部按钮；思考过程改为「流式实时展开、结束自动折叠」；主题设置页自身全面接入令牌（圆角/配色预览/动效预览），AI 文件改动实时同步侧边栏。

### Added

- `[ui]` 会话列表支持左滑露出快捷操作：基于 AnchoredDraggable，左滑后右侧出现「重命名」（蓝）与「删除」（红）两个按钮；删除直接执行并弹出 Snackbar 可撤销，重命名复用现有弹窗；同一时刻仅一行展开，长按完整菜单保留
- `[ui]` 长文本展开编辑面板：输入内容超出单行时右侧出现展开按钮，点击打开双锚点底部面板，支持半屏与全屏两档、顶部拖拽手柄按住切换，面板内多行编辑并与主输入框实时双向同步，底部发送
- `[ui]` 模型输出弹性跟随滚动：位于底部时新内容自动平滑跟随，主动上滑查看历史时暂停跟随不被拉回，滑回底部恢复跟随；不在底部时右下角显示「回到底部」浮动按钮
- `[ui]` 主题设置新增动效预览：动效强度滑块旁提供实时预览动画，拖动滑块可直观感受快慢，0% 即时跳变、100% 最慢

### Changed

- `[ui]` 思考过程流式交互优化：思考持续输出期间默认保持展开并实时跟随，一段思考结束后自动折叠；用户手动展开或折叠的操作始终优先
- `[ui]` 主题预设卡片体验优化：未选中卡片上半部分显示该主题的迷你配色预览（背景/卡片/气泡），「使用中」标签从底部移至右上角角标，所有卡片高度保持一致
- `[ui]` 主题设置页实时预览增强：预览中的输入框补全附件按钮、占位文字与圆形发送按钮，更接近真实对话界面，切换主题/圆角/字号时变化更直观
- `[ui]` 输入框与工具栏间距收紧：文本区上下内边距由 10dp 收紧至 6dp，整体更紧凑（可点击区域保持不变）

### Fixed

- `[ui]` 修复主题设置页自身圆角不跟随（灯下黑）：设置页内卡片、选择器、按钮等全部接入动态圆角令牌，切换圆角/直角/胶囊风格时页面自身实时变化（滑块手柄与三个示意图形保持固定）
- `[ui]` 修复 AI 文件改动不实时同步：AI 在对话中创建、修改、删除文件后，侧边栏工作台文件树立即刷新对应目录，无需退出再进入
- `[ui]` 修复思考中代码块与背景融为一体：代码块背景加深并加强边框，边界清晰可辨
- `[ui]` 修复回复尾部出现大小不一的空白：渲染前清理首尾空段落、合并连续空行，不同消息尾部间距统一
- `[ui]` 修复思考动画在回复未完成时提前消失：流式思考期间在内容末尾保持跳动指示，思考、正文阶段动画平滑交接，不再出现无动画空档或双重打点
- `[ui]` 修复新建对话时上一个对话的输入草稿被带入：草稿按会话隔离，切换或新建会话时清空，不再串对话
- `[ui]` 修复动效强度档位切换无真实效果：补全遗漏的动画消费点，页面切换、卡片展开、弹窗等动效现在跟随强度设置

## [0.0.0.9] - 2026-09-23

> 令牌体系完善与搜索升级版本：全量落地第三层 Component Tokens（13 种组件令牌 + 动态圆角/字号/粗细映射），非标圆角值现在跟随圆角风格切换，顶栏颜色跟随主题预设，设置搜索功能视觉与交互全面升级，全量审计修复 36 处令牌遗漏。

### Changed

- `[ui]` 顶栏颜色跟随主题预设变化：顶栏背景从固定卡片白改为页面底色，切换 6 套主题预设（Default/Cyber/Sunset/Forest/Ocean/Mono）后顶栏与页面融为一体，全局所有页面顶栏同步生效
- `[ui]` 设置搜索视觉与交互升级：搜索框聚焦时品牌色边框与光标、顶栏展开/收起平滑动画（跟随动效强度）、实时结果计数（"找到 N 项"）、搜索历史持久化（KVStore，最多 10 条，可清空）、空结果友好提示、结果按设置分组展示、输入框一键清空按钮、键盘搜索键支持、点击结果自动退出搜索模式
- `[ui]` 全量令牌体系完善：新增第三层 Component Tokens（13 种组件令牌，覆盖卡片/文字/按钮/输入框/列表项/标签/顶栏/分割线/徽章/空状态/对话框/工具卡/气泡），新增 LocalCornerRadius 动态圆角（Sharp/Rounded/Pill 三档），圆角/字号/字体粗细/阴影/内边距大规模令牌化替换

### Fixed

- `[ui]` 非标圆角值现在跟随圆角风格切换：18dp/24dp/28dp/3dp/2dp/20dp 等非标圆角通过 CornerScale.map() 动态映射，Sharp 模式全变直角、Pill 模式全变胶囊、Rounded 模式保持原值，CircleShape 保持圆形不变
- `[ui]` 全量审计修复 36 处令牌遗漏：输入栏 ❯ 符号/光标/工具栏图标色改为主题色、TaskAccordion/FileDiffSheet 的 diff 三色（增/改/删）改为语义色、环境状态徽章改为 success/error、备份状态横幅改为 errorContainer/successContainer/warningContainer、代理状态色改为语义色、TaskCard 阴影改为 PrimitiveElevation

## [0.0.0.8] - 2026-09-23

> 主题设置功能修复与样式打磨版本：修复 v0.0.0.7 主题设置页的 7 项功能缺陷（主题预设颜色不控对话页、背景图无变化、显示偏好无效果、双顶栏、菜单卡片无边距、搜索框常驻、正文首行空行），同步收紧对话流与输入栏间距，搜索交互改为顶栏按钮触发模式。

### Changed

- `[ui]` 设置页搜索交互改为顶栏按钮触发模式：主菜单顶栏右侧显示搜索图标，点击后顶栏展开为搜索输入框（带关闭按钮 + 自动聚焦 + 实时过滤），移除原常驻搜索框，参考 Android 原生设置交互
- `[ui]` 对话流与输入栏间距收紧：消息列表 item 间距从 8dp 收紧至 4dp，上下边距同步收紧，整体视觉更紧凑

### Fixed

- `[ui]` 主题预设颜色现在正确控制对话页面：用户气泡、AI 回复竖线、工具卡片、品牌渐变、Markdown 代码块/链接/引用/表格等全部从硬编码改为读取主题色，切换预设（Default/Cyber/Sunset/Forest/Ocean/Mono）后对话页实时变色；同时补全 Material ColorScheme 映射遗漏字段（outlineVariant/tertiary/errorContainer/secondaryContainer 等）
- `[ui]` 背景图选择后正确显示且重启不丢失：修复根 Surface 不透明遮挡背景图的问题，卡片透明度设置生效，Photo Picker URI 持久化授权确保冷启动后背景图不丢失
- `[ui]` 显示偏好（圆角风格/字体大小/动效强度）现在生效：圆角风格切换后用户气泡和工具块圆角实时变化（圆角/直角/胶囊），字体大小缩放全局文字，动效强度控制脉冲动画速度
- `[ui]` 修复主题设置页双重顶栏：移除 ThemeSettingsScreen 自带的 Scaffold + AppTopAppBar，复用 SettingsScreen 外层顶栏，与其他设置子页保持一致
- `[ui]` 修复设置菜单卡片左右紧贴屏幕边缘：菜单分组卡片增加 16dp 水平边距
- `[ui]` 修复 AI 回复正文首行永远空行：过滤 Markdown 分段器生成的开头空白段，正文从第一行开始显示

## [0.0.0.7] - 2026-09-23

> UI 统一规范化版本：建立完整的 Design Token 体系（Primitive + Semantic），新增 9 个基础组件库，逐模块替换硬编码间距/圆角，新增主题设置页（6 套主题预设 + 9 项颜色自定义 + 背景图 + 显示偏好 + WCAG 对比度警告），支持外观模式切换和实时预览。

### Added

- `[ui]` Design Token 基础设施：PrimitiveColors（8色系×10色阶）、PrimitiveSpacing/Radius/Elevation/Alpha、SemanticColors（50个语义色字段，亮/暗双模式）、AppThemeState + LocalAppTheme，现有调用点零改动
- `[ui]` 基础组件库：AppCard（4变体）、AppListItem、AppChip（3变体×6色）、AppSectionHeader、AppDivider、AppEmptyState、AppButton（4变体×5色×3尺寸）、AppTopAppBar（API兼容）、AppBadge/AppStatusDot，每个组件含 @Preview
- `[ui]` 主题设置页第一期：外观模式（AUTO/LIGHT/DARK）、6 套主题预设（Default/Cyber/Sunset/Forest/Ocean/Mono）、实时聊天预览、KVStore 持久化
- `[ui]` 主题设置页第二期：9 项可自定义颜色（主色/背景/卡片/工具块/文字/边框/错误/成功）、WCAG 对比度警告（<4.5:1 提示）、背景图（系统 Photo Picker + 遮罩浓度 + 卡片透明度）、显示偏好（圆角风格/字体大小/动效强度）、恢复出厂主题

### Changed

- `[ui]` 逐模块硬编码 Token 化：agent/settings/terminal/git/proxy/workspace/capability/core/theme 共 33 个文件，~142 处间距/圆角替换为 Spacing/Radius Token，视觉零变化
- `[ui]` AboutSection 试点：9 个颜色字段替换为 Semantic Token 引用，视觉零变化

### Fixed

- `[ui]` 主题切换时 Material3 组件颜色同步：自定义 SemanticColors 动态映射到 Material ColorScheme，MaterialTheme.colorScheme 组件自动跟随主题变化

## [0.0.0.6] - 2026-09-22

> CI 全链路修复 + 工具卡片样式统一版本：修复 Android Release CI 的 bash 语法错误确保全量构建通过，统一对话流中所有工具调用卡片的渲染样式（消除 `TOOL→REASONING→REPLY` 序列下的兜底简洁样式）。本版本同时清理了非标准版本号 `manual-116`（手动触发 CI 时误创建的 Release，其 CI 修复变更已合并入本版本）。

### Fixed

- `[ci]` 修复 `android-release.yml` 第 180 行 `declare -A` 关联数组语法错误：元素分隔符从分号 `;` 改为空格，bash 严格模式下不再报 `syntax error near unexpected token`，Android Release CI 全量构建（assembleRelease + 签名 + 上传 APK）现已通过。
- `[ui]` 统一工具卡片渲染样式：修复 `TOOL→REASONING→REPLY` 消息序列时，工具调用因 REASONING 触发 `flushPendingTools()` 而走兜底独立 TOOL 单元（绿色圆点 + usage 简洁样式）的问题。现在 REASONING 不再触发 flush，暂存的工具调用作为独立 `EmbeddedToolAccordion` 渲染单元放在思考过程之前，所有工具卡片统一使用带标题（"N 次工具调用"）+ 锤子图标的样式，渲染顺序保持不变（工具调用 → 思考过程 → AI 文本）。

## [0.0.0.5] - 2026-09-22

> 混合模式 v2 样式迭代版本：去掉对话流外层多余气泡容器（TaskAccordion 白色卡片/外层头部/子容器标签头全部去除），消息直接在页面背景上展示；消息尾部新增时间戳显示；输入栏优化（">" 符号与光标同行、边距收紧、模式按钮恢复文字标签）。

### Changed

- `[ui]` 去掉对话流外层气泡：TaskAccordion 白色大卡片改透明（去边框/阴影），去掉外层头部（图标/标题/N条/时间/展开箭头）和"用户消息"/"回复"标签头（图标/文字/序号/箭头），消息直接在页面背景上展示，组间距 16dp，展开/折叠逻辑保留且默认展开。
- `[ui]` 消息尾部新增时间戳显示（HH:mm 格式，10sp 弱化色，右对齐，复用已有 `timestamp` 字段），快捷按钮常驻轻量化（图标间距收紧至 4dp）。
- `[ui]` 输入栏优化：左侧 ">" 符号与输入框改同一行（Row 垂直居中），上下边距收紧至 10dp，模式按钮从圆形图标恢复为胶囊形文字标签（BUILD/PLAN/AUTO，复用项目 ChatAccent 三色语义色，点击切换逻辑保留）。

## [0.0.0.4] - 2026-09-22

> 混合模式对话流样式打磨 + CI 修复版本：以 Cursor/Claude 设计经验为参考，将对话流重构为「用户品牌色气泡 + AI 无气泡左侧竖线 + 工具独立暗色块」的混合模式，同步修复 CI 全部失败（YAML 语法错误 + android-actions 不兼容），CI 现已全绿。

### Changed

- `[ui]` 对话流混合模式样式打磨：用户消息轻量品牌色气泡（#3B82F6，右下小圆角指向用户），AI 回复去掉气泡改左侧 2dp 蓝色竖线标识，工具调用独立暗色块（等宽字体 + ANSI 颜色编码：命令绿/输出灰/错误红），思考过程默认折叠条，流量统计轻量化（10sp 右对齐弱化色），去掉 TaskAccordion 外层浅绿/浅蓝容器背景。
- `[ui]` 暗色/亮色模式配色统一调整：暗色页面底 #0F172A、AI 文字 #E2E8F0、工具块 #1E293B；亮色页面底 #F8FAFC、AI 文字 #0F172A、工具块 #F1F5F9；文字对比度均 ≥4.5:1。
- `[ui]` 输入栏视觉升级：圆角 20dp、左侧蓝色 ❯ 符号、BUILD 按钮改为 28dp 圆形品牌色图标按钮、工具栏图标统一 20dp 弱化色。
- `[ui]` Markdown 渲染样式调整：代码块背景 #1E293B、引用块 2dp 蓝色竖线、行内代码浅灰底，适配 AI 无气泡新样式。

### Fixed

- `[ci]` 修复 CI 全部失败：根因是 `ci.yml` 第 69 行 `name: Security gate: CipherDriverFactory not stub` 未加引号，冒号+空格被严格 YAML 解析器当作嵌套 mapping，导致 workflow 解析立即失败（零 job 创建）。同时替换 `android-actions/setup-android@v3`（与 GitHub Runner Node 24 不兼容），改用 ubuntu-latest 预装 SDK + sdkmanager 手动补装方案。受影响文件：ci.yml / android-release.yml / dependency-audit.yml / weekly-health-check.yml。CI 现已全绿（Build release + Run unit tests 全部通过）。

## [0.0.0.3] - 2026-09-22

> Agent-First 对话流重构版本：以智能体为中心的任务协作界面，新增工具调用卡片（ToolCallCard）与任务卡片（TaskCard），覆盖 6 种工具状态与 7 种任务状态，配套错误诊断、重试机制与样式清爽化。所有新组件通过 feature flag 双开关控制，默认关闭，现有行为完全不变。

### Added

- `[ui]` 新增 Agent-First 工具调用卡片（ToolCallCard）：6 种状态（待执行/运行中/成功/失败/已取消/超时），折叠/展开、实时输出自动滚动与「↓有新输出」提示、大输出截断、复制命令/输出、环境状态条。
- `[ui]` 新增 Agent-First 任务卡片（TaskCard）：7 种状态（空闲/规划中/运行中/等待审批/已完成/失败/已取消），步骤列表与进度条（Running 脉冲高亮）、子分组渲染（用户/回复/工具/思考）、文件产物区域、底部操作栏（停止/查看日志/重试任务）。
- `[agent]` 新增工具调用重试（retryTool）与任务重试（retryTask）：失败的工具调用可一键重试，失败的 Agent 任务可从原始用户请求重新执行。
- `[agent]` 新增工具执行错误诊断：内置 8 种常见错误模式识别（apk 数据库锁/命令未找到/权限不足/磁盘不足等），提供修复建议与一键复制修复命令（建议性，不自动执行）。
- `[ui]` 新增 feature flag 双开关（`useNewToolCallCard` / `useNewTaskCard`）：独立控制新组件启用，默认关闭，可逐步灰度。

### Changed

- `[ui]` AI 对话气泡轻量化：左对齐限宽 90%、去除 1dp 边框改 2dp 轻投影、内边距增大至 16/12dp、圆角增大至 14dp。
- `[ui]` 暗色色阶重定义：页面底 #060D17、AI 气泡 #0E1A2B、工具气泡 #152438、输入栏 #1A2D44；亮色页面底 #F8FAFC、工具气泡 #F1F5F9；文字对比度均 ≥4.5:1。
- `[ui]` 连续同角色消息视觉分组：组内 4dp 间距与小圆角、组间 16dp 间距与大圆角，单条消息四角大圆角。
- `[ui]` 输入栏视觉分离：输入框 12dp 圆角 + 0.5dp 淡边框、发送/附件/模式切换按钮图标统一 20dp。

### Fixed

- `[agent]` 修复 P0：Agent 任务/工具调用 Running 状态推导失效 — `TaskGroup.isStreaming` 仅在文本流式时为 true，工具执行期间状态推导额外检查 `runningTool` 匹配，确保工具执行期间正确显示 Running 状态而非误判为 Completed。

## [0.0.0.2] - 2026-09-22

> 安全加固版本：落地数据库全库加密基础设施（P1 阶段），修复 SQLCipher 空实现与加密失败静默降级两项 P0 安全缺陷。默认仍为明文，用户可在设置页手动开启加密；P2/P3 阶段将逐步推进默认加密与强制加密。

### Added

- `[db]` 数据库全库加密支持（SQLCipher 4.5.4）：6 个物理库可选 AES-256 加密，默认明文，设置页一键开启后自动迁移全部存量数据。
- `[db]` 数据库加密迁移引擎：明文↔加密无损迁移，7 步流程（迁移前快照 → 类型感知逐表拷贝 → 行数+校验和+抽样三重校验 → renameTo 原子替换 → 清理），支持反向迁移（加密→明文）。
- `[db]` 迁移崩溃恢复：启动时自动检测迁移中断状态并安全回滚，覆盖快照/拷贝/校验/替换全流程 10 个崩溃时间点，操作仅限文件系统不打开数据库（< 100ms）。
- `[db]` 双层密钥管理：Android Keystore MasterKey（AES-256-GCM，硬件-backed）包裹每库独立 DEK，包裹密文存 SharedPreferences，密钥不落明文、不出 Keystore。
- `[security]` 新增 `SECURITY.md` 安全策略文档。

### Changed

- `[db]` 数据层 L0 引擎新增 `RoutingDriverFactory`：根据每库加密状态动态选择明文/加密驱动，业务层、迁移引擎、备份全部无感知。
- `[db]` Application 启动流程集成 `CrashRecovery`：首次数据库访问前执行崩溃恢复，确保迁移中断后数据不损坏。
- `[settings]` 安全设置页新增「数据库加密」区块：加密状态显示、开启/关闭开关、迁移进度条（当前库/当前表/百分比）、失败重试按钮。
- `[build]` 新增 `net.zetetic:android-database-sqlcipher:4.5.4` 依赖与 ProGuard keep 规则。
- `[ci]` CI 新增 `CipherDriverFactory` 空实现静态检测门禁，阻止加密占位实现合入。

### Security

- `[security]` 修复 P0：SQLCipher 加密为空实现 — `CipherDriverFactory.create()` 从 `error()` 占位改为完整实现（`SupportFactory` + fail-close + passphrase 用后 `fill(0)` 内存擦除）。
- `[security]` 修复 P0：加密初始化失败静默降级明文 — 密钥管理严格 fail-close，密钥获取/驱动创建任何环节失败抛 `DatabaseEncryptionException`，绝不回退明文或写空串。
- `[docs]` 修正 `README.md` / `AGENTS.md` 中「SQLCipher 加密读写」虚假声明，改为真实状态描述（P1 可选加密，默认明文）。

## [0.0.0.1] - 2026-09-14

> ⚠️ 首个版本（项目初始化），承载完整能力基线，无迁移前状态可比。

### Added

- `[core]` 初始化 MiniMe-core：运行于 Android 真机/虚拟机的 AI 编程工具（`app` / `terminal-emulator` / `terminal-view` / `newui` 四模块 + 双层数据仓库 `datalayer`）。
- `[agent]` 内置 AI Agent，可读写文件、执行 Shell、运行构建；支持多 Provider 适配（Anthropic / OpenAI / Gemini）。
- `[container]` 内置 PRoot + Alpine Linux 容器与终端（arm64-v8a / x86_64 双架构，运行时按宿主选择）。
- `[terminal]` 本地 Termux 终端（`terminal-emulator` + `terminal-view`）+ `newui` 全新设计系统/UI 层。
- `[remote]` 远程 SSH 后端（exec channel + SFTP 文件 + 交互终端会话）。
- `[mcp]` MCP（Model Context Protocol）客户端 + 内置 MCP 服务器（应用成为「客户端 + 服务器」双角色，手机当开发后端）。
- `[git]` Git 集成与可视化操作；`credentials` 集中式凭据管理（UI Git / AI Bash / 终端 git 三端共用）。
- `[backup]` AES 加密备份与恢复；`workspace` 工作区与文档管理。

### Changed

- `[build]` 四段式版本号由 Git Tag 动态推导（`0.0.0.1`），唯一官方签名密钥入库，CI 一键发版。
- `[db]` 数据层采用 SQLDelight V2 六库拓扑（KV / Document / Queue / Blob / TimeSeries + 业务聚合）。

### Security

- `[security]` 凭据加密存储（`CredentialEncryptor` / `DEKManager`）+ HostKey 校验 + AES 备份加密，敏感信息不落明文。